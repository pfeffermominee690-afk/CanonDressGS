# Full Avatar O03 Micro-Pilot Blocker Resolution Contract

The preflight can proceed only after one immutable contract selects all four items below.

1. **Split and denominator.** Select rotation 0, 1, 2, or 3, or explicitly authorize all four O03 views as a view-transductive adaptation/evaluation set. The latter requires revising the current prohibition on target-test RGB entering optimization.
2. **Loss.** Select either the Base Avatar reconstruction loss or `CAPACITY_ORACLE_LOSS_V1`, or provide a complete alternative. Record every term, weight, mask role, crop, color space, and reduction denominator.
3. **Optimizer initialization.** Select restoration of the Base checkpoint optimizer/scheduler states or fresh initialization. Record every optimizer class, parameter group, learning rate, weight decay, scheduler, gradient clip, and whether the checkpoint's historical moments are retained.
4. **Seed.** Select one exact integer seed for the authorized one-run pilot. Base seed `0` and Teacher seed `20260718` are provenance values, not an automatic tie-break rule.

The resolution must retain O03, 1,200 optimizer steps, one run, no sweep, no silent retry, and the existing target-space metric protocol.
