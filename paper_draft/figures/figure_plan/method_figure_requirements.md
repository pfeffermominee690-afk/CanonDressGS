# Method Figure Requirements

Status: `METHOD_FREEZE_PENDING_HEADROOM_AND_LOO`

## Required visual elements

- A provenance-bound canonical avatar render from an existing sealed result.
- Fixed garment-reference thumbnails with identity, garment, view, and source HEAD.
- Explicit residual basis elements grounded in the implemented Gaussian channels.
- A coefficient-space plotting interface whose data source is structured JSON.
- Renderer pipeline nodes limited to modules present in source and frozen contracts.
- Clear visual separation between endpoint prediction, optional coefficient refinement,
  LOO adaptation, and rendering.

## Layout wireframe

Left to right: fixed references, frozen feature extraction, explicit residual basis,
coefficient prediction/refinement interface, residual application, renderer output.
Dashed pending boxes are required around the refinement and LOO routes until their
formal result tasks are sealed.

The figure must not invent an unimplemented module or freeze a method before the
Headroom and LOO decisions. No generated pipeline artwork or third-party media is
permitted. `PAPER_FINAL=0`.
