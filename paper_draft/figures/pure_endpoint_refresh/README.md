# Pure Endpoint Figure Refresh

This directory contains append-only figure evidence derived from the sealed
Pure Endpoint result at
`research/pure-endpoint-core-method-crossfit-amended-20260724@ce110887a942cf8db082ba688c8d36d2433bfdbe`.

All images are review candidates or contact sheets. None is `PAPER_FINAL`.
Scientific renders were not regenerated or retouched. Source render bytes stay
in the sealed external attempt; this directory stores deterministic derivatives
and SHA-addressed provenance.

Key boundaries:

- fixed Subject02 and a closed five-garment wardrobe;
- seen garment references and discrete endpoint realization only;
- hard-lookup relationships are descriptive;
- Headroom and LOO results remain pending;
- historical Figure 3/4 evidence is not mixed with this protocol.

The `provenance/` registries link each selected derivative to its source path,
source SHA, transform, selection rule, and claim boundary.
