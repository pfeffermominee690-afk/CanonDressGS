# Supplementary Caption Skeleton

Aggregate Pure Endpoint parity and safety evidence. Evaluated clean methods
reach top-1 1.0, while fields without endpoint-prediction semantics are marked
N/A. CanonDressGS-Endpoint realizes the registered endpoint exactly; renderer
input and physical-render parity pass on all 20 parity rows; identity,
component, and severe wrong-outfit counts are zero across 120 visual sheets;
and complete dropout safely abstains in 80 of 80 records. Cache reuse is
3,160 of 4,620 logical renders.

Boundary: engineering parity and registered safety evidence. Same-endpoint
parity is not an image-quality superiority claim.
