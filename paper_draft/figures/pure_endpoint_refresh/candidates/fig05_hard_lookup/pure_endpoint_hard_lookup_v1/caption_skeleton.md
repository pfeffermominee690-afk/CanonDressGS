# Figure 5 Caption Skeleton

All clean endpoint methods select the same endpoint and therefore produce
functionally equivalent renders. The clean panel reports endpoint agreement
over 60 queries per comparison and renderer-input/physical-render parity over
20 registered parity rows. Differences under the registered perturbations are
reported descriptively with their denominators. The shared raw predictor has
large raw coefficient error; closed-bank endpoint success is obtained through
nearest-endpoint realization rather than exact continuous coefficient recovery.

Boundary: `HARD_LOOKUP_RELATION_DESCRIPTIVE_ONLY` and
`PERTURBATION_ROBUSTNESS_EVALUATED_DESCRIPTIVE_ONLY`.
