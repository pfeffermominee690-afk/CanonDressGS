\section{Related Work}

\paragraph{Animatable Gaussian avatars.}
The final manuscript should position the fixed-avatar representation relative to animatable Gaussian human rendering and canonical-space deformation. \citationtodo{Use only entries from a supplied and reviewed repository bibliography; no bibliography file was present at audit time.}

\paragraph{Garment-conditioned human rendering and editing.}
The relevant distinction is between synthesizing a broadly variable dressed human and controlling a closed wardrobe of seen garments for one fixed avatar. The latter is the only setting evaluated here. \citationtodo{Add supported references for garment-conditioned avatars, neural wardrobe control, and reference-guided human editing.}

\paragraph{Explicit bases and conditional residual fields.}
Our technical framing separates a finite explicit residual basis from a reference-conditioned coefficient map. The final comparison should discuss linear subspaces, blendshape-style representations, and conditional neural fields without suggesting that $K=4$ is a strong compression result. \citationtodo{Add supported references for explicit deformation/appearance bases and conditional residual representations.}

\paragraph{Retrieval and classification baselines.}
Because all evaluated garments are registered and seen, classification and nearest-centroid retrieval are central baselines rather than peripheral checks. \citationtodo{Add supported references only if they clarify the distinction between retrieval, classification, and continuous control.}

This section intentionally contains no citation commands or fabricated BibTeX entries. It must be completed only after a bibliography asset is supplied and audited.
