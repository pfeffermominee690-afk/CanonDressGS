# Coefficient Headroom Execution Binding

The frozen protocol at `2a42143f7942752aead16e7d53d1b7376fc5a143` is bound to `research/render-refined-coefficient-headroom-experiment-20260724` before any optimizer creation.
The execution contains 120 fresh runs, 36,000 optimizer steps, 960 checkpoints, and one append-only `attempt_001`.

The Pure Endpoint source at `ce110887a942cf8db082ba688c8d36d2433bfdbe` is read-only and is used only for Refined Hard Lookup decomposition. Teacher Endpoint is an initialization and frozen comparison target, not an upper bound. `PAPER_FINAL=false`.
