# Teacher Span Analysis

No Teacher-span analysis was performed. Static asset checks passed, but formal Teacher/SVD parity did not complete because its first output directory was absent.

`COEFFICIENT_HEADROOM_GAIN`, `TEACHER_HEADROOM_GAIN`, `FULL_RESIDUAL_GAIN`, and `SPAN_RECOVERY_RATIO` are all `NOT_EVALUATED`. No denominator was fabricated. `PAPER_FINAL=false`.
