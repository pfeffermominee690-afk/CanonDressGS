# V7 残差解码器容量重设计与验证报告（2026-07-20）

## 1. 任务边界与当前状态

- 任务：`SUBJECT02-RESIDUAL-DECODER-CAPACITY-V7-001`
- 来源提交：`b1dcc16f0d2a57f56ed04778792db8f11f92e081`
- 分支：`research/residual-decoder-capacity-v7-20260720`
- 当前状态：正式 Stage A 已在干净云端完成 1000 optimizer steps，数值与实际视觉验收均为 FAIL；按预注册停止规则未运行 Stage B，最终裁决为 `V7-D`。
- 本任务不修改 renderer、200k Gaussian support、MMLP-Human、Oracle residual、目标图、六属性 bounds 或旧实验输出。

## 2. 为什么必须区分 support 容量和 decoder 容量

Representation Triage 的 Rung-2 已证明原始共享 200k canonical Gaussian support 能在相同相机和 renderer 下表达 O01/O08，因此“点云是否能表示服装”与“条件网络能否产生正确 residual field”是两个不同问题。旧 Probe D 在可实现的 Oracle residual 上仍只有 `0.312990` normalized RMSE、`0.220043` outfit separation 和 `0.156142` garment render MAE；同时所有 latent、FiLM、shared decoder 与 head 梯度均有限非零且无 bound saturation。这把瓶颈定位在旧 decoder/parameterization，而不是 support 或梯度。

## 3. V7 架构

```text
frozen canonical Gaussian attributes + anchor assignment
        -> static support descriptor -> shared small support encoder
reference/global feature -----------------------------+
interpolated completed local feature (direct path) ---+-> concatenate
                                                        |-> geometry trunk
                                                        |    -> xyz head
                                                        |    -> scaling head
                                                        |    -> rotation head
                                                        +-> appearance trunk
                                                             -> opacity head
                                                             -> SH0 head
                                                             -> SHN head
six raw heads -> legacy-identical tanh/vector bound -> fixed-one probe gate
```

Geometry 和 appearance 只共享小型 support encoder；进入 residual trunk 后参数完全独立。六个 final head 均独立且严格零初始化。每个 trunk 使用 LayerNorm、SiLU 和 3–4 个 residual MLP block；local/global condition 在输入和中段各注入一次。LayerNorm 只沿单行 feature 归一化，因此不会因 chunk 边界改变语义。

## 4. Static support descriptor

每个 Gaussian 的 descriptor 仅由冻结 canonical support 构建：

- normalized canonical xyz；
- base log scaling、opacity logit、SH0；
- anchor interpolation 的 max weight、entropy、L2；
- 插值 canonical anchor xyz、Gaussian-anchor 差值与距离；
- canonical xyz Fourier encoding。

它不包含 outfit ID、target RGB/mask、target-view feature、teacher active mask 或 O01/O08 分支。descriptor 由 base 和 topology 可复建，作为非持久 buffer 不重复写入 checkpoint；其字段、维度、归一化中心和尺度写入正式 input audit。

## 5. 条件注入与 gate

V7 直接把 completed local anchor feature 插值到每个 Gaussian，再与 expanded global feature 和 support embedding 拼接。global condition 可用于调制但不替代 local path。Stage A 固定 `geometry_gate=appearance_gate=1`，将 gate 学习与 residual 容量隔离；模块保留显式 external gate 接口供后续正式方法使用。

## 6. 六属性 bounds 与损失

V7 沿用 legacy 合同：xyz `0.05`、log scaling `0.35`、rotation `0.2617993878`、opacity logit `2.0`、SH0 `0.25`、SHN `0.10`。raw head 经 `tanh`（rotation 使用同一 vector-norm 映射）再乘既定 bound。Oracle regression 在 bound-normalized residual space 中计算，Oracle tensor 冻结并 detach，只进入 loss/离线渲染，不进入 prediction forward。

## 7. Stage A：diagnostic latent capacity

正式有效候选为 `attempt_002`；`attempt_001` 在 0 optimizer step 因验收工具未提前创建 render 目录失败，已作为工具级失败证据原样保留。`attempt_002` 完成全部 1000 steps，训练段耗时 `52.6424 s`，峰值显存 `2,984,507,392 bytes`（`2.780 GiB`）。V7 参数量 `410,774`，legacy decoder 参数量 `107,765`，为 `3.812×`。

训练损失里程碑为：step 0 `0.159790`、step 100 `0.142342`、step 300 `0.134364`、step 600 `0.130770`、step 1000 `0.128102`。first-100 均值 `0.149069`，final-100 均值 `0.128991`，下降 `13.4689%`，未达到 `80%`。

| 指标 | O01 | O08 | 均值 / 汇总 | 阈值 | 结果 |
|---|---:|---:|---:|---:|---|
| normalized RMSE | 0.265639 | 0.322713 | 0.294176 | ≤ 0.15 | FAIL |
| normalized MAE | 0.132787 | 0.173393 | 0.153090 | 报告项 | — |
| direction cosine | 0.309016 | 0.388243 | 0.348629 | ≥ 0.65 | FAIL |
| top-10% overlap | 0.419183 | 0.468200 | 0.443692 | ≥ 0.40 | PASS |
| top-20% overlap | 0.513504 | 0.564079 | 0.538792 | 报告项 | — |
| nonzero precision | — | — | 0.943260 | 报告项 | — |
| nonzero recall | — | — | 1.000000 | 报告项 | — |
| spatial concentration in Oracle top-10 | 0.178839 | 0.180469 | 0.179654 | 报告项 | — |
| bound-hit fraction | 0.002073 | 0.011122 | 0.006598 | 报告项 | — |

预测的 outfit separation ratio 为 `0.338673`，低于 `0.50`。四视角平均 garment RGB MAE：O01 `0.086100`、O08 `0.138350`，两者均未达到 `≤0.08`。八个视角的 protected RGB MAE 均值为 `0.016525`，background RGB MAE 均值为 `0.002293`，alpha MAE 均值为 `0.015130`。

与旧 Probe D 相比，V7 mean RMSE 从 `0.312990` 改善到 `0.294176`，separation 从 `0.220043` 改善到 `0.338673`，两套服装平均 garment MAE 从 `0.156142` 改善到 `0.112225`。这些改善不是零，但没有达到预注册的容量闭合阈值，不能据此宣称 decoder capacity 已解决。

support encoder、geometry branch、appearance branch、xyz/scaling/rotation/opacity/SH0 heads 以及 diagnostic latents 的梯度均有限非零。SHN Oracle target 和 prediction 均严格为零，因此 SHN head 梯度为有限的 `0`；这不会改变多个独立硬阈值已失败的裁决。base Gaussian 与 image backbone 前后 SHA256 指纹严格相同，base gradient count、image-backbone gradient count、legacy MLP gradient count 均为 `0`。checkpoint 对 decoder、diagnostic latent、optimizer、RNG、global step、next outfit 和固定输出的恢复均 exact；step-1000 checkpoint SHA256 为 `6701ca0137e34c4f07d4d4642bb2b38787036ab5e86f453025102354d4de940e`。

## 8. Stage B：真实 reference teacher regression

未运行。Stage A 数值和视觉均 FAIL，按冻结合同禁止进入 Stage B。因此 correct-vs-swapped margin、8 episode 胜率和 reference-collapse 指标均为 `NOT_RUN_STAGE_A_FAILED`，不能从 Stage A diagnostic latent 结果推断。Stage A prediction forward 没有使用 target image；正式接口边界测试继续通过。

## 9. Residual support 与 render 对比

Codex 已用本地图像查看器实际打开 `attempt_002` 的 O01/O08 四视角 contact sheet 和两张 residual-support 图，共四张关键图：

- O01 的 V7 final 虽不同于 base/legacy，并形成浅灰白色 hoodie-like 外观，但 torso 与 limbs 周围存在明显 speckle/cloud，未恢复 Oracle 的干净服装结构；
- O08 更弥散，灰色斑驳覆盖身体与腿部，无法干净恢复目标针织上衣与长裤结构；
- 两套服装均违反“不出现大面积 cloud 或 mottle”的硬条件；
- residual-support 图显示 Oracle top-10 只有部分重合，同时存在大量 FP/FN，V7 的低强度 residual 广泛分布于 canonical body，空间特异性不足；
- face 和 shoes 相对 garment 更稳定，但 O08 仍可见广泛低强度泄漏。

因此 Stage A 视觉判定为 **FAIL**。该结论来自实际打开图片后的具体观察，而不是由数值自动推断。

## 10. 最终 Case 与下一任务

- Stage A FAIL：`V7-D`，下一唯一任务 `REASSESS_RESIDUAL_FIELD_PARAMETERIZATION`。
- Stage A PASS、Stage B FAIL：`V7-C`，下一唯一任务 `REDESIGN_REFERENCE_TO_SUPPORT_FEATURE_FUSION`。
- Stage A/B 均 PASS：`V7-P`，下一唯一任务 `VALIDATE_V7_IMAGE_SPACE_OBJECTIVE_FROM_TEACHER_INITIALIZATION`。

正式运行 commit 为 `fa9d97bef5ebba2d50fb1de4758395132dc10e28`。有效输出为：

`/root/autodl-tmp/canondressgs_work/outputs/pipeline_full/SUBJECT02-RESIDUAL-DECODER-CAPACITY-V7-001/attempt_002`

Stage A 数值与视觉均 FAIL，Stage B 按规则未运行，最终 Case 为 **`V7-D`**。结论是当前 V7 仍不能充分表达成功 canonical residual；这不是 renderer、shared 200k support、Oracle、数据或 target image 的重新否定。当前不允许进入 image-space objective 验证。下一唯一任务固定为 **`REASSESS_RESIDUAL_FIELD_PARAMETERIZATION`**，范围仅限 direct per-Gaussian vs anchor interpolation、output rank 和 support descriptor。

## 11. 测试、可复现性与封存

V7 专项合同测试、O01 contract、failure diagnosis、two-outfit prep、dataset、checkpoint、residual、renderer、Python `py_compile` 和 `git diff --check` 均已执行通过。专项测试额外约束 finalization 必须接收实际视觉检查记录，不能在未打开图片时写入视觉验收。Stage A 使用 fixed-one geometry/appearance gates、严格 O01/O08 交替、bound-normalized teacher regression、无 image-space target loss，并保持 target image 不进入 prediction forward。
