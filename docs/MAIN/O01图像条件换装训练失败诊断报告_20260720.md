# O01 图像条件换装训练失败诊断报告（2026-07-20）

## 1. 正式结论

- 任务：`SUBJECT02-IMAGE-CONDITIONED-FAILURE-DIAGNOSIS-001`
- 正式有效结果：`attempt_002`
- 正式运行 commit：`9a5d3142d918fae0103bea4244427638308f3f01`
- 被诊断的 O01 checkpoint：step 2000，SHA256 `335fbfe86bfbcc27dbdb3c99c47a4a3fac3dc55a06bd144e5d674b03a1252838`
- 最终分类：**Case ND — `DECODER_RESIDUAL_CAPACITY_FAILURE`**
- 下一唯一任务：`REDESIGN_RESIDUAL_DECODER_CAPACITY`
- 是否允许直接开始正式 O01/O08 长训练：**否**

这不是数据泄漏、梯度断开、base 更新、checkpoint 恢复、非有限数值或未完成训练造成的失败。Reference 特征和 residual 会随输入变化，因此也不是严格意义上的 `REFERENCE_IGNORED`；主要失败位于 reference-conditioned feature 之后的 residual decoder / parameterization：它不能在已冻结的 300-step diagnostic latent Probe D 中清楚拟合并区分两个已经被 Rung-2 Oracle 证明可实现的服装 residual。

## 2. 封存输入与尝试记录

正式输入均通过绝对路径和 SHA256 校验：

| 输入 | SHA256 |
|---|---|
| O01 step-2000 checkpoint | `335fbfe86bfbcc27dbdb3c99c47a4a3fac3dc55a06bd144e5d674b03a1252838` |
| O01 Rung-2 Oracle checkpoint | `af730d138697ab9c7a29f17603303ae41dfa36047b8cee59e2ee89328655ce56` |
| O08 Rung-2 Oracle checkpoint | `4b0d113cf2e42904ec4f96833e5440fa4f6e6ab013e090be514ef3d8d3dd354e` |
| 当前 base checkpoint | `abbf67b59eadf2cba2dea69dbeec598f9177da45b8ddc74ccbe8108acf9ddf70` |
| full dataset manifest | `49bee929edc236af8f37d66be50eb7c44f4011d49e435af903c0e15918a83bbf` |
| stable protected attribution | `4b6267aad721bbe1a690d631d8479a69859f02058b0b507399fcef448333271d` |

历史 base 指纹 `64ac7f2d…` 与当前 `abbf67b5…` 保持明确区分，未视为同一 checkpoint。

`attempt_001` 在 Probe D 完成 2 个 diagnostic optimizer steps 后触发工具错误：Oracle target 保留了 autograd graph，在第三次、即重新访问 O01 target 时尝试二次 backward。它不是模型失败。该 attempt 原样保留；工具随后冻结并 detach Oracle target，新增回归测试，并从冻结输入新建 `attempt_002`，未继承无效 optimizer 状态。

## 3. S0：当前 checkpoint 四视角静态复核

四视角均使用同一个 step-2000 checkpoint、原协议 reference 和冻结 target pose/camera。结果精确复现原失败：

| 指标（四视角均值） | 值 |
|---|---:|
| edit reduction | 0.352026 |
| target-closer fraction | 0.585765 |
| protected RGB MAE | 0.018737 |
| background RGB MAE | 0.001784 |

左视角 `cond_000017` 的失败是真实的，不是统计分母错误：

- edit reduction：0.031721
- edit base MAE：0.095183
- edit prediction MAE：0.092163
- target-garment：167,941 pixels
- old-garment-removal：141 pixels
- trusted-expansion：6,088 pixels
- trusted-removal：934 pixels
- 上述 hard regions 按 V6.1 contract 互斥，因此实际 edit union 为 **175,104 pixels**，RGB 分母为 **525,312 elements**。
- reference 可见 anchor 数：4,823 / 4,414 / 3,217；任一 reference 被观察到的 anchor 为 8,745。
- geometry gate mean / saturation：0.920275 / 0.629500。
- appearance gate mean / saturation：0.822900 / 0.503700。
- abnormal Gaussian fraction：0.515620。

实际视觉检查显示四个 prediction 均保留紫色/绿斑旧衣外观；左视角没有形成纯灰 O01 hoodie。

## 4. Reference sensitivity

固定 target 为 `cond_000017`（left），target pose/camera 始终不变，target RGB/mask 不进入 forward。

| 干预（相对 R0） | completed feature normalized L2 | residual normalized RMSE | rendered RGB MAE |
|---|---:|---:|---:|
| R1 reference permutation | 1.08e-7 | 7.80e-8 | 1.79e-8 |
| R2 同一 reference 重复三次 | 0.988878 | 0.287018 | 0.017418 |
| R3 只保留一个 reference | 0.988879 | 0.287018 | 0.017418 |
| R4 reference RGB 置零、mask 保持 | 0.080655 | 0.057282 | 0.002698 |
| R5 替换为 base RGB | 0.023711 | 0.014966 | 0.000792 |
| R6 替换为 O08 references | 0.426043 | 0.110598 | 0.004937 |
| R7 确定性轻微颜色扰动 | 0.000551 | 0.000423 | 2.61e-5 |

裁决：

- `REFERENCE_IGNORED = false`。R4/R5/R6 均产生非零 feature、gate、residual 和 render 变化。
- 但 reference-to-render 响应明显偏弱：O08 swap 已让 completed feature 改变 0.426、residual 改变 0.111，最终 RGB 仅改变 0.00494，视觉仍基本相同。
- reference permutation 近似不变符合 weighted/mean set aggregation 的顺序不变性，不是异常。
- R2 与 R3 结果相同，是因为“三次复制同一 reference”和“单 reference”在归一化聚合下等价。

## 5. 当前网络与成功 Oracle residual 的差异

四视角平均的 network-to-O01-Oracle 归一化 RMSE 为 **0.713850**，跨通道平均方向相似度仅 **0.060055**。这说明问题不是只差一个统一尺度，而是方向、位置和通道分配都不正确。

| 通道 | normalized RMSE | cosine | top-10% support overlap | network bound-hit |
|---|---:|---:|---:|---:|
| xyz | 0.553553 | 0.136594 | 0.157338 | 0.024415 |
| log-scaling | 0.769820 | 0.047994 | 0.120350 | 0.436211 |
| rotation | 0.582006 | 0.000760 | 0.102637 | 0.006036 |
| opacity | 0.764368 | -0.052698 | 0.069363 | 0.279531 |
| SH0 | 0.937284 | 0.227681 | 0.128425 | 0.472486 |
| SHN | 0.676068 | 0.000000 | 0.097788 | 0.472505 |

值得注意：Oracle 的 SHN 为零，但当前网络在 SHN 上仍有 47.25% Gaussian 命中 bound；这是没有 Oracle 对应依据的广泛高频外观修改。当前网络与 Oracle 的 garment-region RGB MAE 为 0.0867–0.1036（四视角均值约 0.0971）。视觉上 Oracle 已形成稳定纯灰服装，而当前网络继续呈现旧衣图案和 mottled artifacts。

## 6. Head counterfactual

| 反事实 | target garment RGB MAE | base garment RGB MAE | protected RGB MAE |
|---|---:|---:|---:|
| H0 full | 0.099248 | 0.173441 | 0.018737 |
| H1 xyz only | 0.126680 | 0.151750 | 0.018645 |
| H2 geometry（xyz/scale/rotation） | 0.120903 | 0.153765 | 0.018200 |
| H3 appearance（opacity/SH0/SHN） | 0.133381 | 0.061186 | 0.011253 |
| H4 xyz/opacity/SH0 | 0.112247 | 0.167087 | 0.019169 |
| H5 zero geometry | 0.133381 | 0.061186 | 0.011253 |
| H6 zero appearance | 0.120903 | 0.153765 | 0.018200 |
| H7 clamp gates to [0.1, 0.9] | 0.098608 | 0.163346 | 0.018292 |

没有一个单独 head 能形成目标服装。Geometry-only 保留较多旧衣；appearance-only 主要产生宽泛重着色并更接近 base；二者组合虽降低 pixel MAE，却形成 mottled artifact。把 gate 从饱和区间 clamp 到 `[0.1, 0.9]` 只把 target MAE 从 0.099248 改为 0.098608，不能消除根因，因此 gate saturation 不是首要解释。

## 7. Probe D：decoder capacity

Probe D 冻结 encoder/projector/aggregator/completion、frozen image backbone 和 MMLP-Human，只训练两个 diagnostic outfit latent 与现有 FiLM/HyperNetwork/shared decoder/six residual heads。Oracle residual 只作为 loss/offline target，未进入 formal prediction forward；target RGB/mask 未进入 forward。

- optimizer steps：300（O01/O08 严格交替）
- O01 loss：0.133587 → 0.118750
- O08 loss：0.192323 → 0.164782
- mean loss drop：**0.130028**，低于预注册 0.70
- final normalized RMSE：0.312990，通过单项上限 0.35
- predicted/oracle outfit separation ratio：**0.220043**，低于 0.25
- mean Oracle-render garment RGB MAE：**0.156142**，高于 0.08
- mean head bound-hit：0.0；失败不是再次饱和造成的
- diagnostic latent、FiLM、shared decoder 和六个 heads 的梯度均非零且有限
- Probe D checkpoint SHA256：`047f41cca06bfa9aae31f581213bce07e04cbd1a4c158d693625b2e503f48f60`

视觉上，Probe D 的 O01/O08 输出仍高度相似并被紫色 O01 旧衣主导，不能恢复各自纯灰 Oracle。故按预注册判据，Probe D **FAIL**，状态为 `DECODER_RESIDUAL_CAPACITY_FAILURE`。

## 8. Probe C / Probe R

- Probe C：**NOT RUN**。只有 Probe D PASS 才允许测试真实 reference conditioning；本轮没有绕过门槛。
- Probe R：**NOT RUN**。只有 Probe C PASS 才允许测试 image-space objective drift。

这两个 `NOT RUN` 不能表述为 PASS 或 FAIL，也不能据此下结论说 objective 或 initialization 已排除。当前已足够证明，在更上游的 conditioning/objective 研究之前，现有 residual decoder capacity 必须先闭合。

## 9. 冻结与边界证据

- frozen MMLP-Human base：before/after fingerprint 均为 `de312ccbcabaa37cde63cd318d087b69e830ed5148779ca40456784b964f80d9`；bitwise unchanged；gradient count 0。
- frozen image backbone：before/after fingerprint 均为 `7508f59e7519e2283316c7f484ed9a65b8576ec77351a5d628c6f23c54351efb`；bitwise unchanged；gradient count 0。
- target image/mask、outfit ID、cloth ID、teacher 和 Oracle residual 均未进入正式 forward。
- O01 `attempt_004`、两套服装准备分支、Rung-2 Oracle outputs 均未修改。

## 10. 测试与最终状态

- 新增诊断契约测试：18/18 PASS。
- O01 overfit contract：10/10 PASS。
- 两套服装准备 contract：26/26 PASS。
- full training checkpoint：PASS。
- full Gaussian residual contract：PASS。
- image-conditioned dataset checks：PASS。
- `py_compile`：PASS。
- `git diff --check`：PASS。
- 5 张关键 contact sheet 已由 Codex 实际打开；诊断视觉证据状态：PASS。

最终判定为 **Case ND / `DECODER_RESIDUAL_CAPACITY_FAILURE`**。下一阶段只应执行 `REDESIGN_RESIDUAL_DECODER_CAPACITY` 的小型、预注册 capacity closure；在该闭合通过前，不开始正式 O01/O08 长训练。
